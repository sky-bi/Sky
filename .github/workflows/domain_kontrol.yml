name: Domain Kontrol ve Güncelleme

on:
  workflow_dispatch:  # Manuel çalıştırma
  schedule:
    - cron: "*/2 * * * *"  # Her 2 dakikada bir çalıştırılır.

jobs:
  kontrol:
    runs-on: ubuntu-latest

    steps:
      # Depoyu klonla
      - name: Depoyu Klonla
        uses: actions/checkout@v4

      # Python 3.11'i kur
      - name: Python 3.11 Kur
        uses: actions/setup-python@v5
        with:
          python-version: 3.11

      # Gerekli kütüphaneleri yükle
      - name: Gereksinimleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install requests

      # Betiği çalıştır
      - name: Betiği Çalıştır
        run: python kontrol.py

      # Değişiklik kontrolü yap
      - name: Değişiklik Kontrolü
        id: degisiklik
        run: |
          git diff --quiet || echo "degisiklik=true" >> $GITHUB_OUTPUT

      # Değişiklik varsa commit ve push
      - name: Commit ve Push
        if: steps.degisiklik.outputs.degisiklik == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add test
          git commit -m "🔄 Domain güncellendi: bir sonraki numaraya artırıldı"
          git push origin HEAD
